{
	"Print to console": {
		"scope": "cpp",
		"prefix": "spfa",
		"body": [
			"auto spfa = [&](int s, int n)",
			"{",
			"    auto dist = std::vector(n + 1, lim<int>::max());",
			"    auto update = std::vector<int>(n + 1);",
			"    auto enter = std::vector<bool>(n + 1);",
			"    auto queue = std::queue<int>();",
			"",
			"    dist[s] = 0, update[s] = 1, enter[s] = true;",
			"    queue.push(s);",
			"    while (!queue.empty())",
			"    {",
			"        int u = queue.front();",
			"        queue.pop();",
			"",
			"        enter[u] = false;",
			"        for (auto [v, w]: g[u])",
			"        {",
			"            if (dist[u] + w < dist[v])",
			"            {",
			"                dist[v] = dist[u] + w;",
			"                if (!enter[v])",
			"                {",
			"                    if (++update[v] > n)",
			"                    {",
			"                        return std::nullopt;",
			"                    }",
			"",
			"                    queue.push(v);",
			"                    enter[v] = true;",
			"                }",
			"            }",
			"        }",
			"    }",
			"",
			"    return std::optional(dist);",
			"};"
		],
		"description": "spfa"
	}
}
