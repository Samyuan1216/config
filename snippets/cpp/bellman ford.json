{
	"Print to console": {
		"scope": "cpp",
		"prefix": "bellman_ford",
		"body": [
			"auto bellman_ford = [&](int s)",
			"{",
			"    auto dist = std::vector(n + 1, lim<int>::max());",
			"    dist[s] = 0;",
			"",
			"    for (int i = 1; i < n; ++i)",
			"    {",
			"        bool update = false;",
			"        for (int u = 1; u <= n; ++u)",
			"        {",
			"            for (auto &[v, w]: g[u])",
			"            {",
			"                if (dist[u] != lim<int>::max() && dist[u] + w < dist[v])",
			"                {",
			"                    dist[v] = dist[u] + w;",
			"                    update = true;",
			"                }",
			"            }",
			"        }",
			"",
			"        if (!update)",
			"        {",
			"            break;",
			"        }",
			"    }",
			"",
			"    for (int u = 1; u <= n; ++u)",
			"    {",
			"        for (auto &[v, w]: g[u])",
			"        {",
			"            if (dist[u] != lim<int>::max() && dist[u] + w < dist[v])",
			"            {",
			"                return std::nullopt;",
			"            }",
			"        }",
			"    }",
			"",
			"    return std::optional(dist);",
			"};"
		],
		"description": "bellman_ford"
	}
}
