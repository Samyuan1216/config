{
	"Print to console": {
		"scope": "cpp",
		"prefix": "lca2",
		"body": [
			"auto qg = std::vector(n + 1, std::vector<std::pair<int, int>>());",
			"",
			"auto father = std::vector<int>(n + 1);",
			"for (int i = 0; i <= n; ++i)",
			"{",
			"    father[i] = i;",
			"}",
			"",
			"auto find = [&](auto &&self, int i) -> int",
			"{",
			"    if (i != father[i])",
			"    {",
			"        father[i] = self(self, father[i]);",
			"    }",
			"",
			"    return father[i];",
			"};",
			"",
			"auto visited = std::vector<bool>(n + 1);",
			"auto ans = std::vector<int>(m + 1);",
			"auto tarjan = [&](auto &&self, int u, int f)",
			"{",
			"    visited[u] = true;",
			"    for (auto &v: g[u])",
			"    {",
			"        if (v != f)",
			"        {",
			"            self(self, v, u);",
			"",
			"            father[v] = u;",
			"        }",
			"    }",
			"",
			"    for (auto &[v, i]: qg[u])",
			"    {",
			"        if (visited[v])",
			"        {",
			"            ans[i] = find(find, v);",
			"        }",
			"    }",
			"};"
		],
		"description": "lca2"
	}
}
