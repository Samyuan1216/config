{
	"Print to console": {
		"scope": "cpp",
		"prefix": "Diagonal_Dist",
		"body": [
			"auto diagonal_dist = [&](int a, int b, int x, int y) -> int",
			"{",
			"    return std::max(std::abs(x - a), std::abs(y - b));",
			"};"
		],
		"description": "对角线距离"
	}
}
